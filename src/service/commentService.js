const { Users } = require('../models/users')
const { Comments } = require('../models/comments')

const commentService = {
    // 특정 게시물 전체 댓글 조회
    fetchList : (res, req) => {

    },
    
    // 특정 게시물 댓글 생성
    createComment: (res, req) => {

    },

    // 특정 게시물 댓글 수정 
    updateComment: (res, req) => {

    },

    // 특정 게시물 댓글 삭제
    deleteComment: (res, req) => {

    },

}

module.exports = {
    commentService
}